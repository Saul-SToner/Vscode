# archive 目录说明

原先仓库内的反演样本目录 `archive/inversion_examples/` 已整体移出仓库，备份到：

```text
C:\Users\L2791\thinfilm_backups\inversion_examples_20260505
```

当前 `archive/` 不再存放反演样本 CSV。

## 当前保留内容

- `figures/`：旧版汇报或对比展示图，避免继续堆在项目根目录。
- `scripts/`：一次性临时绘图脚本，不作为主运行入口。
